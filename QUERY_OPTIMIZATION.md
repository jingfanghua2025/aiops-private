# 查询类问题优化 - 精准回答，不生成多余步骤

## 问题分析

用户问题："查看下这台服务器docker 跑的mysql root密码是多少"

**问题**：
- 系统生成了9个步骤
- 实际上只需要2步：找到容器 + 查看环境变量
- 密码在第4步就已经找到了，但系统继续生成了5个不必要的步骤

**根本原因**：
- 提示词没有区分"查询类问题"和"操作类问题"
- 没有限制查询类问题的步骤数量
- 系统倾向于"为了完整性"生成大量检查步骤

## 优化方案

### 1. 识别查询类问题

查询类问题的特征：
- 包含关键词："是多少"、"是什么"、"密码"、"配置"、"状态"、"查看"、"显示"、"列出"
- 用户意图是**获取信息**，而不是**执行操作**

### 2. 限制查询类问题的步骤数

**规则**：
- 查询类问题：最多生成2步
  - 第1步：找到目标（容器/文件/服务等）
  - 第2步：查询信息（环境变量/配置文件/状态等）
- 操作类问题：根据复杂度生成必要步骤

### 3. 优化提示词

**新增内容**：
1. 明确区分查询类和操作类问题
2. 查询类问题最多2步的硬性限制
3. 提供错误示例（9步）和正确示例（2步）对比
4. 在用户提示词中识别问题类型并限制步骤

## 优化后的行为

### 用户问题："mysql root密码是多少"

**优化前**（错误）：
```json
{
  "steps": [
    {"desc": "查找mysql容器", "cmd": "..."},
    {"desc": "查找mariadb容器", "cmd": "..."},  // ❌ 用户没问
    {"desc": "查找所有数据库容器", "cmd": "..."},  // ❌ 用户没问
    {"desc": "检查环境变量", "cmd": "..."},  // ✅ 这个是对的
    {"desc": "检查启动命令", "cmd": "..."},  // ❌ 不需要
    {"desc": "检查配置文件", "cmd": "..."},  // ❌ 不需要
    {"desc": "检查日志", "cmd": "..."},  // ❌ 不需要
    {"desc": "验证端口", "cmd": "..."},  // ❌ 不需要
    {"desc": "检查docker-compose", "cmd": "..."}  // ❌ 不需要
  ]
}
```

**优化后**（正确）：
```json
{
  "steps": [
    {
      "desc": "查找MySQL容器",
      "cmd": "C=$(docker ps --filter 'name=mysql' --format '{{.Names}}' | head -1) && if [ -n \"$C\" ]; then echo \"找到容器: $C\"; else echo \"未找到MySQL容器\" && exit 1; fi 2>&1"
    },
    {
      "desc": "查看MySQL root密码",
      "cmd": "C=$(docker ps --filter 'name=mysql' --format '{{.Names}}' | head -1) && if [ -n \"$C\" ]; then docker exec $C env | grep MYSQL_ROOT_PASSWORD 2>&1 || docker inspect $C | grep -A1 MYSQL_ROOT_PASSWORD 2>&1; else echo \"未找到MySQL容器\"; fi"
    }
  ]
}
```

## 其他查询类问题示例

### "查看README.md文件内容"
**优化后**：1步
```json
{
  "steps": [
    {"desc": "查看README.md文件内容", "cmd": "cat README.md 2>&1 || echo '文件不存在'"}
  ]
}
```

### "检查Docker容器状态"
**优化后**：1步
```json
{
  "steps": [
    {"desc": "查看Docker容器状态", "cmd": "docker ps -a 2>&1"}
  ]
}
```

### "查找graf容器"
**优化后**：1步
```json
{
  "steps": [
    {"desc": "查找包含graf关键字的容器", "cmd": "docker ps --filter 'name=graf' --format '{{.Names}} {{.ID}} {{.Ports}}' 2>&1 || echo '无匹配容器: graf'"}
  ]
}
```

## 代码变更

### 修改文件
- `app/services/enhanced_ssh_service.py`
  - `_build_system_prompt()`: 添加查询类问题的限制和示例
  - `_build_user_prompt()`: 识别查询类问题并限制步骤数

### 关键改进点

1. **系统提示词**：
   - 添加"查询类问题最多2步"的硬性限制
   - 提供错误示例（9步）和正确示例（2步）对比
   - 明确禁止生成大量检查步骤

2. **用户提示词**：
   - 自动识别查询类问题（通过关键词）
   - 明确标注问题类型
   - 强调"最多2步"的限制

## 测试建议

1. **查询密码**：
   - 问题："mysql root密码是多少"
   - 预期：最多2步（找到容器 + 查看密码）

2. **查询配置**：
   - 问题："查看nginx配置文件"
   - 预期：最多2步（找到容器/文件 + 查看内容）

3. **查询状态**：
   - 问题："检查容器是否运行"
   - 预期：1步（检查状态）

4. **操作类问题**（不受限制）：
   - 问题："重启所有容器"
   - 预期：根据复杂度生成必要步骤

## 部署

代码已更新，重启服务后生效：

```bash
pkill -f '/usr/bin/python3 -m uvicorn app.main:app'
cd /data/aiops
nohup /usr/bin/python3 -m uvicorn app.main:app --host 0.0.0.0 --port 80 > /data/aiops/aiops-backend.log 2>&1 &
```

## 效果

- ✅ 查询类问题：最多2步，精准回答
- ✅ 操作类问题：根据复杂度生成必要步骤
- ✅ 不再生成大量不必要的检查步骤
- ✅ 100%专注用户需求

