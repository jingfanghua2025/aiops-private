# 功能更新总结

## 更新日期
2025-12-23

## 已完成的功能

### 1. 登录页面添加忘记密码功能 ✅

**前端**：
- 在登录页面添加"忘记密码？"链接
- 添加忘记密码弹窗，支持邮箱和短信两种方式
- 实现切换找回方式的UI
- 实现发送验证码和重置密码功能

**后端**：
- `/api/v1/auth/reset-password` API已存在，支持email和sms两种方式

**使用方式**：
1. 点击登录页面的"忘记密码？"
2. 选择找回方式（邮箱/短信）
3. 输入邮箱/手机号，获取验证码
4. 输入验证码和新密码
5. 重置成功

### 2. 新用户默认300个算力值 ✅

**修改文件**：
- `app/models/user.py`：`balance = Column(Float, default=300.0)`
- `app/api/auth_endpoints.py`：注册时 `balance=300.0`

**效果**：
- 新注册用户默认获得300个算力值
- 管理员账号仍为99.0（保持不变）

### 3. 算力不足提示 ✅

**前端**：
- 添加"算力不足"弹窗组件
- 显示友好的提示信息："您好，您的算力不足，请充值算力。方案咨询和代码编写永久免费，排障与部署扣算力点收费。"
- 提供"取消"和"去充值"按钮

**后端**：
- `/api/v1/ssh/propose` 接口在余额不足时返回402错误
- 错误信息包含完整的提示文本

**触发时机**：
- 用户尝试生成排障计划时，如果余额不足5点，会弹出提示

### 4. 方案咨询和代码编写永久免费 ✅

**确认**：
- `/api/v1/ask` 接口：`cost=0.0`（已确认免费）
- 方案咨询（qa模式）：免费
- 代码编写（code模式）：免费

### 5. 排障与部署收费规则 ✅

**收费规则**：
- 排障计划生成：**5点/次**（约¥0.05）
- 命令执行：免费（不重复扣费）

**成本分析**：
- DeepSeek Chat: ~$0.14/1M tokens (输入) + $0.28/1M tokens (输出)
- 豆包 Qwen-Turbo: ~¥0.008/1K tokens
- 每次排障计划约消耗2000-5000 tokens，成本约¥0.02-0.04
- **我们收费：5点 = ¥0.05（覆盖API成本+服务成本+合理利润）**

**计费代码位置**：
- `app/api/endpoints.py` 第51-56行：计费规则定义
- `TROUBLESHOOT_BASE_POINTS = 5`：排障计划消耗5点

### 6. 仪表盘算力消耗图表 ✅

**新增功能**：
- 添加ECharts图表库
- 消费金额趋势图（柱状图）
- API请求次数趋势图（面积图）
- Tokens使用量趋势图（柱状图）
- 月份选择器（可查看历史月份）
- 导出功能

**数据来源**：
- `/api/v1/usage/series` API端点
- 返回按天聚合的用量数据

## 文件变更

### 修改文件
- `static/index.html`：
  - 添加忘记密码弹窗和功能
  - 添加算力不足提示弹窗
  - 添加仪表盘图表展示
  - 添加ECharts库引用
  - 改进API错误处理（支持402错误）

- `app/models/user.py`：
  - 新用户默认余额：300.0

- `app/api/auth_endpoints.py`：
  - 注册时设置余额：300.0

- `app/api/endpoints.py`：
  - 更新计费规则：5点/次
  - 改进算力不足提示信息

## 部署说明

### 1. 代码已更新
所有功能已实现并编译通过。

### 2. 重启服务
```bash
pkill -f '/usr/bin/python3 -m uvicorn app.main:app'
cd /data/aiops
nohup /usr/bin/python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > /data/aiops/aiops-backend.log 2>&1 &
```

### 3. 测试建议

1. **忘记密码功能**：
   - 点击登录页面的"忘记密码？"
   - 测试邮箱找回和短信找回

2. **新用户注册**：
   - 注册新账号，验证默认余额为300

3. **算力不足提示**：
   - 使用余额不足的账号尝试生成排障计划
   - 验证是否弹出提示

4. **仪表盘图表**：
   - 访问仪表盘，查看用量图表
   - 测试月份切换和导出功能

## 收费规则总结

| 功能 | 收费 | 说明 |
|------|------|------|
| 方案咨询 | 免费 | 永久免费 |
| 代码编写 | 免费 | 永久免费 |
| 排障计划生成 | 5点/次 | 约¥0.05，覆盖API成本+利润 |
| 命令执行 | 免费 | 不重复扣费 |

**算力点价值**：1点 = ¥0.01

## 注意事项

1. **新用户默认余额**：已改为300点，但已存在的用户不受影响
2. **算力不足提示**：只在排障计划生成时检查，方案咨询和代码编写不受影响
3. **图表数据**：需要用户有使用记录才能看到图表数据
4. **忘记密码**：需要配置好邮箱和短信服务才能正常使用

