# AIOps+ 用户计费模式说明

## 计费规则

根据代码分析（`app/api/endpoints.py`），系统的计费模式如下：

### 1. 免费功能（cost = 0.0）

以下功能**完全免费**，不消耗点数：

- **智能问答** (`/api/v1/ask`)
  - 方案咨询模式
  - 代码编写模式
  - 仅记录使用次数，不扣费

- **命令执行** (`/api/v1/ssh/execute`)
  - 执行已生成的命令计划
  - 仅记录执行日志，不扣费

### 2. 计费功能

**排障计划生成** (`/api/v1/ssh/propose`)
- **扣费点数**：3点
- **说明**：生成排障计划时扣费，包含"计划生成 + 1次修正"的打包费用
- **计费时机**：用户点击"排障&部署"模式，选择主机并发送需求后，系统生成命令计划时扣费
- **余额检查**：如果用户余额不足3点，会返回402错误："排障点数不足，请充值后重试"

### 3. 点数价值

- **1点 ≈ 0.005元**（模型成本）
- **标准排障**：3点 ≈ 0.015元

### 4. 充值方式

- **真实支付**：微信支付、支付宝支付（通过 `/api/v1/usage/recharge/order` 创建订单）
- **演示充值**：需要 `ALLOW_DEMO_RECHARGE=true` 才开启（默认关闭）

### 5. 用量记录

所有操作都会记录到 `UsageLog` 表：
- `task`：任务描述
- `cost`：消耗的点数（免费功能为0.0）
- `timestamp`：操作时间

可通过 `/api/v1/usage/history` 查看用量明细。

## 计费逻辑代码位置

- **计费规则定义**：`app/api/endpoints.py` 第51-56行
- **排障计划扣费**：`app/api/endpoints.py` 第667-677行
- **智能问答免费**：`app/api/endpoints.py` 第658行
- **命令执行免费**：`app/api/endpoints.py` 第688行

## 用户余额

- **默认余额**：新注册用户余额为 `1.0` 点（`app/models/user.py` 第41行）
- **管理员默认余额**：`99.0` 点（`app/models/user.py` 第133行）
- **余额查询**：`GET /api/v1/usage/balance`

## 总结

- ✅ **智能问答**：完全免费
- ✅ **命令执行**：完全免费
- 💰 **排障计划生成**：3点/次（约0.015元）

用户主要在使用"排障&部署"功能生成命令计划时需要消耗点数，其他功能都是免费的。

